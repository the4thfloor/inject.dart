name: inject_annotation
version: 1.0.0-alpha.5
description: Compile-time constructor-based dependency injection for Dart and Flutter, similar to Dagger.
repository: https://github.com/ralph-bergmann/inject.dart
issue_tracker: https://github.com/ralph-bergmann/inject.dart/issues

environment:
  sdk: ^3.6.0
resolution: workspace

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.1.1
